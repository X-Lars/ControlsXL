<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:xl="clr-namespace:ControlsXL">

    <Style TargetType="{x:Type xl:VectorButton}">

        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
        
        <Setter Property="Fill" Value="{DynamicResource BackgroundBrush}"/>
        <Setter Property="Stroke" Value="{DynamicResource BorderBrush}"/>

        <Setter Property="Height" Value="Auto"/>
        <Setter Property="Width" Value="Auto"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type xl:VectorButton}">
                    
                    <!-- VECTOR BUTTON BORDER -->
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            RenderTransformOrigin="0.5, 0.5">

                        <!-- VECTOR BUTTON IMAGE -->
                        <Path x:Name="VectorImage"
                              Data="{TemplateBinding Vector}"
                              DataContext="{Binding DataContext, RelativeSource={RelativeSource TemplatedParent}}"
                              Fill="{TemplateBinding Fill}"
                              Stroke="{TemplateBinding Stroke}"
                              StrokeThickness="{TemplateBinding StrokeThickness}"
                              Stretch="Uniform"
                              RenderTransformOrigin="0.5, 0.5">

                            <Path.RenderTransform>
                                <ScaleTransform ScaleX="{Binding Scale, RelativeSource={RelativeSource TemplatedParent}}"
                                                ScaleY="{Binding Scale, RelativeSource={RelativeSource TemplatedParent}}"/>
                            </Path.RenderTransform>
                            
                        </Path>

                    </Border>

                    <ControlTemplate.Triggers>

                        <!-- MOUSE OVER -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="VectorImage" Property="Fill" Value="{DynamicResource MouseOverBackgroundBrush}"/>
                            <Setter TargetName="VectorImage" Property="Stroke" Value="{DynamicResource MouseOverBorderBrush}"/>
                        </Trigger>

                        <!-- MOUSE PRESS -->
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="VectorImage" Property="Fill" Value="{DynamicResource PressedBackgroundBrush}"/>
                            <Setter TargetName="VectorImage" Property="Stroke" Value="{DynamicResource PressedBorderBrush}"/>
                        </Trigger>
                        
                        <!-- DISABLED -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="VectorImage" Property="Fill" Value="{DynamicResource DisabledBackgroundBrush}"/>
                            <Setter TargetName="VectorImage" Property="Stroke" Value="{DynamicResource DisabledBorderBrush}"/>
                        </Trigger>

                        <!-- IS CHECKED -->
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="VectorImage" Property="Fill" Value="{DynamicResource CheckedBackgroundBrush}"/>
                            <Setter TargetName="VectorImage" Property="Stroke" Value="{DynamicResource CheckedForegroundBrush}"/>
                        </Trigger>
                        
                    </ControlTemplate.Triggers>
                    
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>