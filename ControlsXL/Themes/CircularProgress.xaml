<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:xl="clr-namespace:ControlsXL"
                    xmlns:element="clr-namespace:ControlsXL.Elements">
    
    <Style x:Name="CircularProgress" TargetType="{x:Type xl:CircularProgress}">
        
        <Setter Property="Height" Value="46"/>
        <Setter Property="Width" Value="{Binding ActualHeight, RelativeSource={RelativeSource Self}}"/>
        
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type xl:CircularProgress}">

                    <!-- CIRCULARPROGRESS: BORDER-->
                    <Border BorderThickness="{DynamicResource {ComponentResourceKey xl:Styles, BorderThickness}}"
                            BorderBrush="Transparent">
                        
                        <!-- CIRCULARPROGRESS: LAYOUT -->
                        <Grid>

                            <!-- CIRCULARPROGRESS: BACKGROUND ARC -->
                            <element:Arc End="{TemplateBinding EndAngle}"
                                         Stroke="{DynamicResource {ComponentResourceKey xl:Styles, ControlBrush}}"
                                         StrokeThickness="{TemplateBinding Thickness}"
                                         Start="0"
                                         RenderTransformOrigin="0.5, 0.5">
                                
                                <element:Arc.RenderTransform>
                                    <RotateTransform Angle="{Binding StartAngle, RelativeSource={RelativeSource TemplatedParent}}"/>
                                </element:Arc.RenderTransform>
                                
                            </element:Arc>

                            <!-- CIRCULARPROGRESS: PROGRESS ARC -->
                            <element:Arc End="{TemplateBinding Angle}"
                                         Stroke="{DynamicResource {ComponentResourceKey xl:Styles, ControlSelectedBrush}}"
                                         StrokeThickness="{TemplateBinding Thickness}"
                                         Start="0"
                                         RenderTransformOrigin="0.5, 0.5">
                                
                                <element:Arc.RenderTransform>
                                    <RotateTransform Angle="{Binding StartAngle, RelativeSource={RelativeSource TemplatedParent}}"/>
                                </element:Arc.RenderTransform>
                                
                            </element:Arc>

                            <!-- CIRCULARPROGRESS: TEXT VALUE -->
                            <TextBlock x:Name="Value" 
                                       FontSize="10"
                                       Foreground="{DynamicResource {ComponentResourceKey xl:Styles, TextBrush}}"
                                       HorizontalAlignment="Center" 
                                       RenderTransformOrigin="0.5, 0.5"
                                       Text="{TemplateBinding Text}"
                                       VerticalAlignment="Center"/>

                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>

                        <Trigger Property="ShowValue" Value="false">
                            <Setter TargetName="Value" Property="Visibility" Value="Collapsed"/>
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>