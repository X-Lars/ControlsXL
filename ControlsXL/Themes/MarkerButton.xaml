<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:xl="clr-namespace:ControlsXL">

    <Style TargetType="{x:Type xl:MarkerButton}">
        
        <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="{DynamicResource BorderThickness}"/>

        <Setter Property="Template">
            <Setter.Value>
                
                <ControlTemplate TargetType="{x:Type xl:MarkerButton}">
                    
                    <!-- MARKER BUTTON BORDER -->
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        
                        <!-- MARKER BUTTON CONTENT -->
                        <DockPanel>
                            
                            <!-- MARKER -->
                            <Border x:Name="Marker"
                                    DockPanel.Dock="{Binding MarkerPlacement, RelativeSource={RelativeSource TemplatedParent}}"
                                    Background="{TemplateBinding BorderBrush}"
                                    Height="{Binding MarkerSize, RelativeSource={RelativeSource TemplatedParent}}"
                                    Width="{Binding MarkerSize, RelativeSource={RelativeSource TemplatedParent}}"
                                    Focusable="False"/>

                            <!-- TEXT -->
                            <ContentPresenter x:Name="Content"
                                              Focusable="False"                        
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              HorizontalAlignment="Center"
                                              Margin="4, 0"
                                              VerticalAlignment="Center"/>

                        </DockPanel>
                    </Border>
                    
                    <ControlTemplate.Triggers>

                        <!-- MARKER DOCK POSITION LEFT -->
                        <Trigger SourceName="Marker" Property="DockPanel.Dock" Value="Left"> 
                            <Setter TargetName="Marker" Property="Height" Value="{Binding ActualHeight}"/>
                        </Trigger>

                        <!-- MARKER DOCK POSITION RIGHT -->
                        <Trigger SourceName="Marker" Property="DockPanel.Dock" Value="Right">
                            <Setter TargetName="Marker" Property="Height" Value="{Binding ActualHeight}"/>
                        </Trigger>

                        <!-- MARKER DOCK POSITION TOP -->
                        <Trigger SourceName="Marker" Property="DockPanel.Dock" Value="Top">
                            <Setter TargetName="Marker" Property="Width" Value="{Binding ActualWidth}"/>
                        </Trigger>

                        <!-- MARKER DOCK POSITION BOTTOM -->
                        <Trigger SourceName="Marker"  Property="DockPanel.Dock" Value="Bottom">
                            <Setter TargetName="Marker" Property="Width" Value="{Binding ActualWidth}"/>
                        </Trigger>

                        <!-- IS CHECKED -->
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Marker" Property="Background" Value="{DynamicResource CheckedBackgroundBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource CheckedBackgroundBrush}"/>
                        </Trigger>

                        <!-- MOUSE OVER -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Marker" Property="Background" Value="{DynamicResource MouseOverBorderBrush}"/>
                            <Setter Property="Background" Value="{DynamicResource MouseOverBackgroundBrush}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource MouseOverBorderBrush}"/>
                        </Trigger>

                        <!-- MOUSE PRESS -->
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Marker" Property="Background" Value="{DynamicResource PressedBorderBrush}"/>
                            <Setter Property="Background" Value="{DynamicResource PressedBackgroundBrush}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource PressedBorderBrush}"/>
                        </Trigger>

                        <!-- DISABLED -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Marker" Property="Background" Value="{DynamicResource DisabledBorderBrush}"/>
                            <Setter Property="Background" Value="{DynamicResource DisabledBackgroundBrush}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource DisabledBorderBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource DisabledForegroundBrush}"/>
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>