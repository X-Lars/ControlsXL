<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:xl="clr-namespace:ControlsXL">

    <Style TargetType="Thumb" x:Key="ThumbStyle">
        <Setter Property="Background" Value="AliceBlue"/>
        <Setter Property="BorderBrush" Value="Blue"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Height="{TemplateBinding Height}"
                            Margin="-8 0"
                            Width="16">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SliderButtonStyle"
       TargetType="{x:Type RepeatButton}">
        <Setter Property="SnapsToDevicePixels"
          Value="true" />
        <Setter Property="OverridesDefaultStyle"
          Value="true" />
        <Setter Property="IsTabStop"
          Value="false" />
        <Setter Property="Focusable"
          Value="false" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border Background="Transparent" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <ControlTemplate TargetType="xl:Slider" x:Key="HorizontalSlider">
        <Border Background="Pink" 
                BorderBrush="DeepPink"
                BorderThickness="1"
                Height="4"
                Width="{TemplateBinding Width}">
                
            <Grid x:Name="PART_Container">
                <Track>
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource SliderButtonStyle}"/>
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb x:Name="PART_Thumb" Style="{StaticResource ThumbStyle}"/>
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource SliderButtonStyle}"/>
                    </Track.IncreaseRepeatButton>
                </Track>
            </Grid>
        </Border>
    </ControlTemplate>

    <ControlTemplate TargetType="xl:Slider" x:Key="VerticalSlider">
        <Border Background="{DynamicResource {ComponentResourceKey xl:Styles, ControlBackgroundBrush}}" 
                BorderBrush="{DynamicResource {ComponentResourceKey xl:Styles, BorderBrush}}"
                BorderThickness="{DynamicResource {ComponentResourceKey xl:Styles, BorderThickness}}">

            <Grid x:Name="PART_Container">
                <Track>
                    <Track.Thumb>
                    <Thumb x:Name="PART_Thumb" Style="{StaticResource ThumbStyle}"/>
                    </Track.Thumb>
                </Track>
            </Grid>
        </Border>
    </ControlTemplate>

    <Style TargetType="{x:Type xl:Slider}">
        <Setter Property="Background" Value="{DynamicResource {ComponentResourceKey xl:Styles, ControlBackgroundBrush}}"/>
        <Setter Property="MinHeight" Value="21"/>
        <Setter Property="Template" Value="{StaticResource HorizontalSlider}"/>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Template" Value="{StaticResource VerticalSlider}"/>
            </Trigger>
        </Style.Triggers>

    </Style>
</ResourceDictionary>